generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model students {
  id    Int     @id @default(autoincrement())
  name  String?
  age   Int?
  grade String?
  major String?
}